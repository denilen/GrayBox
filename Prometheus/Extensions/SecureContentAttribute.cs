namespace Prometheus.Extensions
{
    /// <summary>
    /// Attribute specify whether model could be configured as hidden in log output  
    /// </summary>
    public class SecureContentAttribute : Attribute
    {
    }
}
